# Server-only URL used by the same-origin /graphql proxy.
GRAPHQL_BACKEND_URL=http://localhost:3773/graphql

# Browser-visible GraphQL subscription endpoint (graphql-transport-ws).
NEXT_PUBLIC_GRAPHQL_WS_URL=ws://localhost:3773/graphql
